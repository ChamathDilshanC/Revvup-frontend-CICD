import type { ImageSourcePropType } from 'react-native';

/** Auth hero — login, register, welcome backgrounds. */
export const AUTH_HERO_IMAGE: ImageSourcePropType = require('../assets/Background/lr_background.jpg');

export const APP_LOGO: ImageSourcePropType = require('../assets/AppLogo.png');
